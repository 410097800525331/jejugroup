allprojects {
}

gradle.projectsLoaded {
  rootProject.allprojects { p ->
    if (p.path == ':') {
      p.tasks.register('runProbeLinks', JavaExec) {
        classpath = p.sourceSets.main.runtimeClasspath + p.files('D:/git/jejugroup/.codex-temp')
        mainClass = 'ProbeLinks'
      }
      p.tasks.register('runProbeDashboard', JavaExec) {
        classpath = p.sourceSets.main.runtimeClasspath + p.files('D:/git/jejugroup/.codex-temp')
        mainClass = 'ProbeDashboard'
      }
    }
  }
}
